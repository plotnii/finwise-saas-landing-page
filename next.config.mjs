/** @type {import('next').NextConfig} */
const nextConfig = {
  i18n: {
    locales: ["en", "ro", "ru"], // Языки: английский, румынский, русский
    defaultLocale: "en", // Язык по умолчанию
  },
};

export default nextConfig;
